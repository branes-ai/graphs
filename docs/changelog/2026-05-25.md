# Changelog 2026-05-25

KPU energy-model calibration cluster (closes #177, #121, #154, #81), two
adjacent enhancements (#122 scaling study, #176 ARM Neoverse N2 reference), and
the CPU-mapper accuracy thread (#175 fanout, #178 L1-spill, #78 package power).
Nine graphs PRs (#251-#255, #257, #259, #260, #261) and two embodied-schemas
PRs (#84, #85). (#175/#178/#78 + test/doc follow-ups landed 2026-05-26..27 as a
same-session continuation.)

## Fixed

- **CPU static power charged at package level, not per-core** (closes #78).
  EnergyAnalyzer's per-unit allocation branch divided the calibrated CPU package
  power across allocated cores and power-gated the rest, but uncore / IMC / ring
  / shared-L3 are package-level and can't be gated -- an i7 4-of-12-core
  allocation charged ~29 W instead of ~87.5 W (worse after the #175 cap reduced
  allocated cores). For CPU, charge the full #71-calibrated idle_power_watts
  regardless of allocation (matches the no-allocation fallback the V4 RAPL
  baseline validated); non-CPU accelerators keep per-unit power gating.
  (`src/graphs/estimation/energy.py`,
  `tests/analysis/test_energy_cpu_uncore_static.py`) [#260]
- **CPU L1-spill compute-utilization haircut for uncalibrated ARM refs**
  (closes #178 fix #2). The CPU matvec ran at ~90% utilization even when the
  4 MB weights were 64x the per-core L1, letting a 5 W single ARM core out-rank
  a 30 W Jetson on batch=1 Linear. `RooflineAnalyzer._cpu_l1_fit_scale` scales
  attainable FLOP util by `(l1_per_core / working_set_per_core) ** 0.2` (working
  set spread across the #175-capped effective cores) -> 90% -> 39% util on the
  matvec; AmpereOne-1core 5997 -> 2655 inf/s, so both Jetsons now out-rank it.
  OPT-IN via `HardwareResourceModel.cpu_l1_spill_haircut` -- enabled only on
  uncalibrated references (AmpereOne 192/128/1core, Neoverse N2); i7 (measured
  baseline) is opted out to avoid double-counting. #178 fix #1 (GPU kernel-launch
  overhead) was already addressed (per-mapper kernel_launch_overhead_ns, cited
  to arXiv:2508.08430); fix #3 (ground-truth calibration) is hardware-gated.
  CPU_L1_SPILL_EXPONENT=0.2 is THEORETICAL pending calibration.
  (`src/graphs/estimation/roofline.py`,
  `src/graphs/hardware/resource_model.py`,
  `src/graphs/hardware/models/datacenter/{cpu_arm,ampere_ampereone_*,arm_neoverse_n2}.py`,
  `tests/hardware/test_cpu_l1_spill_haircut.py`) [#259]

- **CPU per-operator concurrency cap + per-SKU memory bandwidth** (closes #175).
  A many-core CPU let a single operator claim full multi-core fanout (192-core
  AmpereOne reported ~71K inf/s on a batch=1 matvec). Fix #1: roofline compute
  ceiling and CPUMapper allocation now scale by
  `effective_cores = min(num_cores, total_macs / MIN_MACS_PER_CORE)` (64K
  MACs/core floor) -- batch=1 Linear(2048,2048) -> 64/192 on AmpereOne; large /
  batch-parallel / non-CPU / single-core unaffected. Fix #2: `peak_bandwidth_gbps`
  threaded through `cpu_arm_resource_model`; AmpereOne 192/128 set to the 332.8
  GB/s datasheet value (was the 80 GB/s edge default). Scope note: the catalog
  matvec is now overhead-bound (LLC-resident weights via #172 + ~6.5 us/op
  launch overhead), so the headline number barely moves; the cap is correct and
  binds for compute-bound batch=1 ops. (`src/graphs/estimation/roofline.py`,
  `src/graphs/hardware/mappers/cpu.py`,
  `src/graphs/hardware/models/datacenter/{cpu_arm,ampere_ampereone_192,ampere_ampereone_128}.py`,
  `tests/hardware/test_cpu_concurrency_cap.py`) [#257]

- **Thermal-envelope clamp: avg power can never exceed TDP** (closes #177, #121).
  `EnergyAnalyzer` now computes `average_power_w` against a thermally-bound
  latency `max(burst_latency, energy / TDP)`. TDP is a sustained thermal limit,
  so a short burst can't sustain an average above it; energy stays physical
  (truthful joules) and the effective latency floors. Fixed the 983-1175 W
  artifacts for sub-microsecond KPU ops. Universal across mappers; inert unless
  avg power would breach TDP. (`src/graphs/estimation/energy.py`,
  `validation/model_v4/{cli/compare_hardware.py,harness/runner.py,invariants/kpu.py}`,
  `tests/test_energy_thermal_clamp.py`) [#251]
- **KPU leakage now scales with Vdd** (closes #154). `kpu_power_model` scaled
  every dynamic term by `(V/Vnom)^2` but held leakage flat, overstating it 2-3x
  on low-Vdd DVFS profiles. Leakage now scales by
  `(V/Vnom)^leakage_vdd_exponent` (new ProcessNode field, embodied-schemas #84),
  falling back to flat when absent. (`src/graphs/hardware/kpu_power_model.py`,
  `tests/hardware/test_kpu_sku_generator.py`) [#252]
- **KPU dynamic energy single-sourced from ProcessNode; 2x FLOP double-count
  fixed** (closes #81). The loader fed the node's per-MAC FP32 energy straight
  into `energy_per_flop_fp32`, but `sg.flops` counts 2 FLOPs/MAC -> FP32 billed
  at 5.4 pJ/MAC; and `model.energy_scaling` used hardcoded defaults instead of
  node ratios. Now `energy_per_flop_fp32 = node_fp32_per_MAC / 2` and
  `energy_scaling` derives from the node's `energy_per_op_pj` ratios. Effective
  T64: FP32 2.7, INT8 0.30, BF16 1.4 pJ/MAC. FP32/small GEMMs now respect TDP
  unaided; the clamp is a true backstop.
  (`src/graphs/hardware/models/accelerators/kpu_yaml_loader.py`,
  `tests/hardware/test_kpu_energy_single_source.py`) [#253]

## Added

- **KPU family scaling study** (closes #122). `compare_kpu_scaling.py` sweeps
  matmul/vector_add across T64/T128/T256/T768 in BF16 and reports burst +
  thermally-bound latency, energy/inference, efficiency (inf/J == throughput/W),
  and utilization -- 5-panel figure (.png/.pdf) or data table (.csv/.md/.json)
  plus a textual crossover analysis. Registered T128/T256/T768 in
  `SWEEP_HW_TO_MAPPER`. Finding: T128 first beats T64 only at matmul 2048^3
  (wave-quantization saturation below); utilization climbs 53%->70% T64->T768.
  (`validation/model_v4/cli/compare_kpu_scaling.py`,
  `validation/model_v4/harness/runner.py`,
  `tests/validation_model_v4/test_compare_kpu_scaling.py`) [#254]
- **ARM Neoverse N2 single-core reference mapper** (closes #176). A real
  published-IP ARM reference (vs the synthetic AmpereOne 1-core slice) modeling
  N2's 2x 128-bit SVE2 datapath, BF16, INT8 dot-product, 64 KiB L1D, 1 MiB
  private L2, configurable core count. Registered as `ARM-Neoverse-N2-1core`.
  Validation cross-checks ~1.5x FP32 vs the generic ARM helper.
  (`src/graphs/hardware/models/datacenter/arm_neoverse_n2.py`,
  `src/graphs/hardware/mappers/cpu.py`,
  `src/graphs/hardware/mappers/__init__.py`,
  `validation/hardware/test_arm_neoverse_n2.py`) [#255]

## Infrastructure

- Bumped embodied-schemas CI pin `7cc12c4 -> b0e3920` across all 8 ref sites in
  ci / coverage / v4-validation workflows (picks up #84 leakage_vdd_exponent +
  #85 KPU Vdd re-tune). (`.github/workflows/{ci,coverage,v4-validation}.yml`) [#252]
- `avg_power_below_tdp` promoted from a soft/xfail diagnostic to a hard KPU
  invariant; registry PhysicalSpec coverage pin updated to 43 populated /
  5 unpopulated (N2 reference is N/A by data availability).
- Dropped the `validation`-package import from `test_cpu_concurrency_cap` /
  `test_cpu_l1_spill_haircut` (inline `_matmul_sg` from graphs.core); it wasn't
  reliably on sys.path in the xdist `Tests (Python 3.12)` job and broke #260's
  tests/analysis test. (`tests/hardware/test_cpu_*`, `tests/analysis/...`) [#261]

## embodied-schemas (companion PRs)

- **#84**: optional `leakage_vdd_exponent` on `ProcessNodeEntry`
  (tsmc_n16: 4.0, tsmc_n7: 4.5); backward-compatible (default None = flat).
- **#85**: re-tuned the 4 Stillwater KPU SKU profile `vdd_v` values so the
  power model still derives the round envelope TDPs (3/6/10, 15/30/50,
  30/60/100 W) under Vdd-scaled leakage. Vdd-only change; clock/TOPS unchanged.

# Session: 2026-05-25 -- KPU energy-model calibration cluster + scaling study + ARM N2

## Summary

Opened from "what is next" and resolved the entire KPU energy-bug cluster
(#177, #121, #154, #81) plus two adjacent enhancements (#122 scaling study,
#176 ARM Neoverse N2). Shipped 5 graphs PRs (#251-#255) and 2 embodied-schemas
PRs (#84, #85), each CI-green and merged.

Headline arc: the KPU energy model went from physically-impossible (1175 W
predicted on a 6 W part) to calibrated and single-sourced -- thermal clamp as a
true backstop, leakage Vdd-aware, dynamic energy sourced from the process node,
with a family scaling study and a real ARM reference mapper on top.

graphs diff: 21 files, +1100/-64. embodied-schemas: 8 files, +93/-12.

## What happened (in order)

### 1. PR #251 -- thermal-envelope clamp (closes #177, #121)

Investigation showed the avg_power>TDP violation is real: for a dense GEMM the
EnergyAnalyzer assumes ~100% duty while the derived TDP assumes ~15% duty, so
peak compute power (~19.7 W on T64 INT8) legitimately exceeds the 6 W envelope.
The physically-correct treatment is thermal-bound roofline: energy is truthful,
the effective (sustained) latency floors at energy/TDP, so
`average_power_w = energy / thermally_bound_latency <= TDP`.

- EnergyReport gains `thermally_bound_latency_s`, `thermal_throttle_active`,
  `tdp_watts`. total_energy_j and burst total_latency_s stay truthful.
- The #121 reporting path (compare_hardware/runner) reads the clamped sustained
  power (983 W -> 6 W) and surfaces thermally_bound_latency_ms.
- `avg_power_below_tdp` flipped from xfail (#81 diagnostic) to a hard invariant.
- New tests/test_energy_thermal_clamp.py.

### 2. embodied-schemas #84 + #85, graphs #252 -- leakage Vdd scaling (closes #154)

Cross-repo. ES#84 added optional `leakage_vdd_exponent` to ProcessNodeEntry
(n16: 4.0, n7: 4.5, cited FinFET sub-threshold + DIBL). graphs#252 consumes it:
`leakage_w *= (V/Vnom)^exponent`.

Consequence: derived TDP shifts off the round envelope labels. The user chose to
PRESERVE the round catalog TDPs by re-tuning each profile's Vdd (ES#85) rather
than store the new off-round derived values (which was reverted). Vdds solved by
bisection so derivation lands on target within 0.5 W; clock/TOPS untouched. CI
pin bumped 7cc12c4 -> b0e3920 across 8 sites.

### 3. PR #253 -- single-source dynamic energy from ProcessNode (closes #81)

Investigation found #81's stated exit criterion (avg_power invariant) was
already met by #251's clamp, and energy_per_flop was already ~node-consistent.
The real defects: (a) a 2x MAC/FLOP double-count -- node energy_per_op_pj is
per-MAC (the loader feeds the same figure to tile_energy_model as mac_energy)
but sg.flops is 2 FLOPs/MAC; (b) model.energy_scaling used hardcoded dataclass
defaults, not node ratios.

Fix: `energy_per_flop_fp32 = node_fp32_per_MAC / 2`;
`model.energy_scaling.update(node ratios)`. Effective T64 = node values (FP32
2.7, INT8 0.30, BF16 1.4 pJ/MAC). FP32/small GEMMs now fit under TDP unaided;
the clamp fires only for genuinely power-bound dense low-precision GEMM.

### 4. PR #254 -- KPU family scaling study (closes #122)

compare_kpu_scaling.py: 5-panel figure + data table + crossover findings across
the four SKUs in BF16. Registered T128/T256/T768 in SWEEP_HW_TO_MAPPER. Findings
answer the issue: T128 first beats T64 at matmul 2048^3 (tied below = wave-quant
saturation); utilization 53%->70% across the family at 4096^3.

CI caught a matplotlib ModuleNotFoundError in the V4/Tests jobs (no matplotlib
installed); fixed with pytest.importorskip on the render test, mirroring
test_compare_hardware.

### 5. PR #255 -- ARM Neoverse N2 reference mapper (closes #176)

arm_neoverse_n2.py models N2's real per-core datapath (2x 128-bit SVE2, BF16,
INT8 SDOT, 64 KiB L1D, 1 MiB L2). Distinct from the synthetic AmpereOne slice.
Validation: resource-model shape, ResNet-50 batch=1 (68 ms / 14.7 inf/s on one
4 W core), and a cross-check that N2's 2x SVE2 gives ~1.5x the generic helper's
FP32 (scalar unchanged, vector doubled) -- the calibration anchor #176 asked for.
Coverage pin updated (N2 is reference IP with no ComputeProduct YAML).

## Key decisions

- **Thermal-bound latency, not energy-down** (#251): energy is physical; clamp
  the latency. Keeps inf/J honest and total_energy_j truthful.
- **Preserve round catalog TDPs via Vdd re-tune** (#154): the user reverted the
  "store new derived TDP" approach; round envelope labels are the product spec,
  so re-tune (clock, Vdd) to hit them. See DECISION-2026-05-25-001.
- **node energy_per_op_pj is per-MAC** (#81): resolved from the loader's own
  usage (same figure -> tile_energy_model.mac_energy) -> per-FLOP base = /2.
- **Single-source from ProcessNode, not tile_energy_model** (#81): user chose
  the conservative node-physics source over the aggressive domain-flow MAC
  energies; documented both numerically before deciding.

## Files modified (graphs)

- src/graphs/estimation/energy.py (clamp)
- src/graphs/hardware/kpu_power_model.py (leakage Vdd scaling)
- src/graphs/hardware/models/accelerators/kpu_yaml_loader.py (energy single-source)
- src/graphs/hardware/models/datacenter/arm_neoverse_n2.py (new)
- src/graphs/hardware/mappers/cpu.py + __init__.py (N2 factory + registry)
- validation/model_v4/cli/{compare_hardware,compare_kpu_scaling}.py
- validation/model_v4/harness/runner.py (clamped power helper + SWEEP map)
- validation/model_v4/invariants/kpu.py (hard invariant + docstrings)
- validation/hardware/test_arm_neoverse_n2.py (new)
- tests/{test_energy_thermal_clamp, hardware/test_kpu_energy_single_source,
  validation_model_v4/test_compare_kpu_scaling}.py (new)
- tests/hardware/{test_kpu_sku_generator, test_physical_spec_loader}.py
- tests/validation_model_v4/{test_kpu_invariants, test_compare_hardware}.py
- .github/workflows/{ci,coverage,v4-validation}.yml (pin bump)

## Files modified (embodied-schemas)

- src/embodied_schemas/process_node.py (leakage_vdd_exponent field)
- src/embodied_schemas/data/process-nodes/tsmc/{n16,n7}.yaml (exponents)
- src/embodied_schemas/data/compute_products/stillwater/kpu_t*.yaml (Vdd re-tune)
- tests/test_process_node_leakage_vdd_exponent.py (new)

## Tests run

- graphs full suites green throughout (energy/clamp, kpu invariants, hardware,
  v4 validation, integration gate test_all_hardware.py). 1569-1598 passing in
  the affected-suite sweeps.
- embodied-schemas: 962 -> 966 (+4, leakage exponent tests).
- All 5 graphs PRs + 2 ES PRs CI-green and merged.

## Issues closed this session

- graphs#177, #121, #154, #81, #122, #176, #175, #178, #78
- embodied-schemas: leakage_vdd_exponent (#84), KPU Vdd re-tune (#85)

### 6. PR #257 -- CPU batch=1 fanout cap + per-SKU bandwidth (closes #175)

Landed 2026-05-26 as a same-session continuation. Two compounding issues let a
many-core CPU overstate batch=1 throughput.

- Fix #1 (concurrency cap): a single operator only fans out across
  `min(num_cores, total_macs / MIN_MACS_PER_CORE)` cores (64K MACs/core floor).
  `RooflineAnalyzer._cpu_concurrency_scale` (CPU-gated) scales the compute
  ceiling by effective_cores/num_cores; the CPUMapper allocation caps
  cores_allocated the same way. Verified: batch=1 Linear(2048,2048) -> 64/192 on
  AmpereOne (matvec compute_time 0.84us -> 2.5us).
- Fix #2 (bandwidth): peak_bandwidth_gbps threaded through
  cpu_arm_resource_model; AmpereOne 192/128 -> 332.8 GB/s datasheet (was 80).

Diagnostic finding: in the current code (post-#172 LLC stationarity + per-op
overhead) the catalog batch=1 matvec on AmpereOne-192 is OVERHEAD-bound, not
compute-bound -- weights are LLC-resident (~0.08us) and a ~6.5us/op launch
overhead dominates. So the headline 71K inf/s barely moves; the issue's "192x
compute fanout" premise predated those models. Per maintainer decision, shipped
the correct cap + bandwidth fix + a cap-level regression test (not an overhead/
LLC-bandwidth rework). 1781 tests green; AmpereOne validations unchanged (no
regression, confirmed identical pre/post).

### 7. PR #259 -- CPU L1-spill utilization haircut (closes #178)

Landed 2026-05-26. #178 flagged two CPU-over-GPU biases at batch=1. Fix #1 (GPU
per-kernel overhead) was ALREADY addressed: kernel_launch_overhead_ns is
per-mapper (edge 80us / datacenter 10us), cited to arXiv:2508.08430 -- the 80us
is deliberate. Fix #3 (calibration) needs Orin Nano / AGX hardware. Shipped fix
#2: the CPU matvec ran at ~90% util even with the 4MB weights 64x the L1.
`RooflineAnalyzer._cpu_l1_fit_scale` scales util by
`(l1_per_core / working_set_per_core)^0.2` (working set over the #175-capped
effective cores) -> 90% -> 39%; AmpereOne-1core 5997 -> 2655 inf/s, so AGX Orin
(7602) and Orin Nano (5939) now out-rank it.

Key gating lesson: the first cut applied the haircut universally and broke i7's
measured-baseline floor tests (i7's tuned efficiency already reflects real L1).
Gated to OPT-IN via HardwareResourceModel.cpu_l1_spill_haircut, enabled only on
uncalibrated references (AmpereOne 192/128/1core, N2); i7 opted out. 1787 green.

### 8. PR #260 -- CPU package-level static power (closes #78)

Landed 2026-05-26. EnergyAnalyzer's per-unit allocation branch divided CPU
package power across allocated cores and gated the rest, but uncore/IMC/ring/L3
are package-level -- i7 4-of-12 cores charged ~29W not ~87.5W (worse after the
#175 cap). For CPU, charge full #71-calibrated idle_power_watts regardless of
allocation (matches the no-allocation fallback); non-CPU keeps per-unit gating.
1794 green.

CodeRabbit on #260 flagged the EnergyDescriptor UNKNOWN-confidence default;
deferred to #79 (pre-existing, pinned by test_confidence.py:183, needs the
confidence-blending design) -- CodeRabbit concurred. A CI miss (the new
tests/analysis test imported the non-installable `validation` package) was fixed
inline, then #261 removed the same fragile import from the two #257/#259 tests.

## What's next

- KPU + CPU-mapper accuracy thread fully closed (#177/#121/#154/#81/#175/#178/#78).
- **#79** -- EnergyDescriptor/energy estimates default to UNKNOWN confidence even
  when calibrated; surfaced by the #260 review. Needs a confidence-blending design
  (static can be CALIBRATED while compute/memory coefficients are THEORETICAL ->
  honest aggregate is the weakest link) and an update to the test that pins UNKNOWN.
- Remaining levers if a future workload needs the batch=1 CPU headline to move:
  the ~6.5us/op launch overhead and per-operator LLC-bandwidth scaling (deferred).
